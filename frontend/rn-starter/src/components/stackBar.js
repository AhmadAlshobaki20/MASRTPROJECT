import React from "react";
import { StatusBar } from "react-native";

const ComponentStatusBar = () => {
  return (
    <StatusBar
      backgroundColor="red" // Set your desired background color
      barStyle="light-content" // Set the status bar content color (light or dark)
    />
  );
};

export default ComponentStatusBar;
